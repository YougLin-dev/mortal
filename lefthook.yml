pre-commit:
  commands:
    format:
      run: pnpm run format
      stage_fixed: true
    lint:
      run: pnpm run lint
      stage_fixed: true
    check:
      run: pnpm run check

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
